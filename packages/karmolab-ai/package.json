{
  "name": "karmolab-ai",
  "version": "0.1.0",
  "private": true,
  "description": "KarmoLabAI — Google Generative AI SSOT (AI Studio + Vertex): model catalog, REST URLs, docs links",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "~5.9.0"
  }
}
