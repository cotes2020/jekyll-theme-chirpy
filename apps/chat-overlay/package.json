{
  "name": "chat-overlay",
  "version": "0.1.0",
  "private": true,
  "description": "Windows transparent always-on-top chat overlay (MVP)",
  "main": "dist/main.js",
  "type": "module",
  "scripts": {
    "build": "tsc && node scripts/copy-static.mjs",
    "start": "npm run build && electron .",
    "dev": "npm run build && electron .",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "electron": "^33.2.1",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
