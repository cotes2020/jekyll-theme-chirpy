# 레포 루트 공통 (선택) — 비어 있어도 됩니다.
# 욘봇·카카오 스크립트보다 먼저 로드됩니다. 팀·CI에서 여러 앱이 같이 쓰는 값만 두면 됩니다.
#
# 사용: 이 파일을 `.karmolab.common.env` 로 복사
#
# 예시 (필요할 때만 주석 해제):
# NODE_OPTIONS=--max-old-space-size=4096
# NPM_TOKEN=
