/**
 * Populated by `assets/js/data/swconf.js` (Jekyll) and loaded via `importScripts` in the SW.
 */
declare const swconf: {
  purge: boolean;
  cacheName?: string;
  resources?: string[];
  interceptor?: { urlPrefixes: string[]; paths: string[] };
};
