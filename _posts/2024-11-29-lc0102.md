---
title: LP보고 - LPworks 데이터 전송 (lc0102)
author: dkdk.kr
date: 2026-02-03 09:00:00 +0800
categories:
  - LPworks
tags:
  - LPworks
render_with_liquid: true
mermaid: false
---

LPworks 데이터 전송에서는 조합 및 조합의 투자 정보를 LPworks로 전송하는 방법을 설명합니다.

## 설명

### 대상 선택 및 전송
LPworks를 사용하는 출자자에게 조합별로 전송하고자 하는 정보를 선택하여 전송할 수 있습니다.

#### 조합별 데이터 전송
조합별로 데이터를 전송하는 방법은 다음과 같습니다.

1. `1. 대상 조합 선택`에서 정보를 전송하고자 하는 조합을 선택합니다.
	- `청산 조합 표시` 토글을 통해, 청산완료된 조합도 선택할 수 있습니다.
2. `2. 전송 데이터 선택`에서 전송하고자 하는 정보를 선택합니다.
	- 각 항목별 전송 데이터
		- 조합 정보: 조합의 기본 정보, 운용 인력, 회계 감사인 정보를 전송합니다.
		- 출자/배분 및 지분양수도: 조합의 출자/배분 및 지분양수도 거래 내역을 전송합니다.
		- 출자/배분 계획: 조합의 보고기준일별 출자/배분 계획을 전송합니다.
		- 조합 수익률: 조합의 수익률 정보를 전송합니다.
		- 조합 재무제표: 조합의 메인 회계원장에 기록된 재무상태표와 손익계산서 데이터와  이를 기반으로 생성된 재무제표 파일을 전송합니다.
		- 투심위 보고: 조합의 투자심의위원회가 개최된 투자 심의 건 중 보고할 건을 선택하여 전송합니다.
		- 영업보고서: 조합별로 등록한 월/분기/수시 영업보고서를 파일을 전송합니다.
		- 포트폴리오 정보: 조합이 투자한 포트폴리오 및 투자현황 정보를 전송합니다.
		- 포트폴리오 재무제표: 조합이 투자한 포트폴리오의 재무제표 정보를 전송합니다.
		- 포트폴리오 사업현황: 분기별 영업보고서에 작성한 사업 현황 정보를 전송합니다.
3. `데이터 전송`을 클릭해 주세요.
	- [tip] 데이터 전송까지 약 5~10분 가량이 소요됩니다.
	- `전송 이력` 버튼을 클릭해 전송 상태와 전송일시, 전송자를 조회할 수 있습니다.

#### 조합 재무제표 기준일 선택
조합 재무제표 전송 시에 기준일자를 선택하여 전송할 수 있습니다.
1. `2. 전송 데이터 선택`에서 조합 재무제표 `>` 아이콘을 클릭해주세요.
2. 기준일 선택 드로어에서 기준일자를 선택해주세요.
3. `저장` 버튼을 눌러주세요.
	- 데이터 전송 시 해당 조합의 재무제표의 기준일은 선택된 기준일을 바탕으로 다음과 같이 조회 및 첨부파일을 생성하여 전송합니다.
		- 재무상태표 기준일
			- 선택한 기준일
		- 손익계산서 기준기간
			- 당기: 조합 회계연도 초일부터 기준일까지
			- 전기: 이전 회계연도 전체 기간

#### 투심위 보고 대상 선택
투심위 보고 전송 시에 전송하고자 하는 투심위 건을 선택하여 전송할 수 있습니다.
1. `2. 전송 데이터 선택`에서 투심위 보고 `>` 아이콘을 클릭해주세요.
2. 투심위 보고 대상 선택 모달에서 전송하고자 하는 건의 체크박스를 선택해주세요.
	- `투자/회수` > `투자 심의` 메뉴에서 `본투자심의` 단계에 있으며, 투자심의위원회의 개최일시와 장소가 입력된 건만 노출됩니다.
	- 노출된 투자심의 건 중 다음 조건을 모두 만족하는 투자심의 건만 체크박스 선택이 `가능`합니다.
		- [회사정보] 탭의 `국가`, `사업자번호`, `표준산업분류`, `자본금`, `설립일자` 모두 입력
		- [투자정보] 탭의 `투자조건`, `투자 라운드 정보`, '`기타정보`의 상위 4개 항목' 모두 입력
		- [첨부파일] 탭의 `투자심사보고서`, `준법사항체크리스트`, `투심위의사록`(결과보고 시에만) 모두 첨부
		- 재원별 `개최일시`와 `장소`가 입력된 개최 정보 존재
3. `저장` 버튼을 눌러주세요.
	- 데이터 전송 시 체크박스가 선택된 건들만 LPworks로 전송됩니다.
		- 결과보고 값이 없는 건은 계획보고 값이 `가능` 혹은 `완료`인 경우, 계획보고로 분류되어 전송됩니다.
		- 결과보고 값이 있는 건은 결과보고 값이 `가능` 혹은 `완료`인 경우, 결과보고로 분류되어 전송됩니다.
		- 계획보고 혹은 결과보고의 값이 `불가능`인 경우, `불가능` 셀을 클릭하여 해당 투자심의 건으로 이동하여 수정할 수 있습니다.

#### 조합별 영업보고서 첨부
한글 또는 word파일 등으로 생성한 조합별 영업보고서를 직접 첨부하여 전송할 수 있습니다.

1. `2. 전송 데이터 선택`에서 영업보고서 `>` 아이콘을 클릭해주세요.
2. 영업보고서 등록 드로어에서 파일을 첨부할 조합을 클릭해주세요.
3. 기준일과 분기를 선택하신 후, 영업보고서를 첨부해주세요.
	- 하단의 버튼을 통해 행을 제거하거나 추가할 수 있습니다.
4. `저장` 버튼을 눌러주세요.
	- 데이터 전송 시 `영업보고서` 항목을 선택한 경우, 첨부한 파일이 LPworks로 전송됩니다.

## 자주 묻는 질문

> LPworks로 잘 전송됐는지 확인할 수 있는 방법이 있나요?
{: .prompt-tip }
- 상단의 `전송 이력` 버튼을 클릭해 전송 상태를 확인할 수 있습니다.
- 데이터 전송까지 약 5~10분의 가량이 소요될 수 있습니다.

> 대상 조합을 여러 개 선택하면 어떻게 전송되나요?
{: .prompt-tip }
- 1에서 선택한 각 조합에 대해, 2에서 선택한 모든 데이터가 각각 전송됩니다.
  (ex. 3개 조합 x 5개 데이터 → 총 15건 전송)
- LP는 본인이 출자한 조합의 정보만 조회할 수 있습니다.

> 전송항목에 대해 동일한 식별자로 여러번 전송하면 어떻게 되나요?(ex. 동일 조합-보고기준일의 출자/배분 계획 전송)
{: .prompt-tip }
- LPworks에서 LP는 기본적으로 동일한 전송 항목에 대해, 가장 최신 전송된 값만 조회합니다.

> 전송을 취소할 수는 없나요?
{: .prompt-tip }
- 현재 이미 전송 요청된 건을 취소할 수 있는 기능은 제공하지 않고 있습니다.
